import SwiftUI

struct Title: View {
  var body: some View {
    VStack {
      Text("Group Lunch Picker")
        .font(.largeTitle)
        .bold()
      Text("When the collective mind can't decide what takeout to choose")
        .font(.caption)
        .italic()

    }
  }
}
