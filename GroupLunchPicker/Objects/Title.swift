import SwiftUI

struct Title: View {
  var body: some View {
    VStack {
      Text("Group Lunch Picker")
        .font(.largeTitle)
        .bold()
      Text("When you can't decide what takeout order to choose")
        .font(.caption)
        .italic()

    }
  }
}
